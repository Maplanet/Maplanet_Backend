name: CI/CD Pipeline

on:
  pull_request:
    branches:
      - develop

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Set up Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18' # Update this with your desired Node.js version

      - name: Build Docker image
        run: |
          docker build -t parkbrianjunsoo/maplanet_repository:v1 .

      - name: Push Docker image to Docker Hub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Push Docker image
        run: |
          docker push parkbrianjunsoo/maplanet_repository:v1

      - name: Print Secrets
        run: |
          echo ${{ secrets.AWS_EC2_HOST }}
          echo ${{ secrets.AWS_EC2_USERNAME }}
          echo ${{ secrets.AWS_EC2_PRIVATE_KEY }}

      - name: SSH into EC2 instance and deploy
        uses: appleboy/ssh-action@master
        with:
            host: ${{ secrets.AWS_EC2_HOST }}
            username: ${{ secrets.AWS_EC2_USERNAME }}
            key: ${{ secrets.AWS_EC2_PRIVATE_KEY }}
            script: |
              docker pull parkbrianjunsoo/maplanet_repository:v1
              docker stop my-app || true
              docker rm my-app || true
              docker run -d --name my-app -p 80:8080 ${{ secrets.DOCKER_USERNAME }}/maplanet_repository:v1
              EOF